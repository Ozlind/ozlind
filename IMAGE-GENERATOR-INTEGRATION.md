# OZLIND in-chat image generation

Image requests stay inside normal chat. The client calls `/api/image-generate`; the server calls Gemini with `GEMINI_API_KEY`. No image-provider key is exposed to the browser.

Pollinations is not part of this release.
